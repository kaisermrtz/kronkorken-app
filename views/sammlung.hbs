<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{pageTitle}}</title>

    {{> import}}
    <style>
      .sammlung p{
          font-size: 16px;
      }

      .sammlung{
          margin: 5px;
          height: 100%;
          padding-bottom: 5px;
      }

      #sammlungrow{
          margin: 5px;
      }
      #sammlungrow a{
        color: #000000;
        text-decoration: none;
      }

      #sammlungrow div{
          margin-bottom: 5px;
      }
    </style>
  </head>
  <body>
    {{> header}}
    <form id="searchform">
      <input type="text" name="q" placeholder="Suchen" class="form-control">
    </form>
    <div class="row" id="sammlungrow">
      {{#each crowncaps}}
      <a href="/sammlung/{{_id}}">
        <div class="col-md-4 col-sm-6 col-xs-12">
          <div class="sammlung">
            <div class="col-xs-6">
              <img src="/img/kronkorken/{{image}}_resized.jpg" class="img-responsive">
            </div>
            <div class="col-xs-6">
                <h4>{{brand}}</h4>
                <p>{{name}}</p>
                <p>{{country}}</p>
                <p>{{tags}}</p>
            </div>
          </div>
        </div>
      </a>
      {{/each}}
    </div>
    {{> footer}}
  </body>
</html>
